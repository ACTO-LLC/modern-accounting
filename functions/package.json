{
    "name": "accounting-functions",
    "version": "1.0.0",
    "description": "Azure Functions for Modern Accounting payroll and tax calculations",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "prestart": "npm run build",
        "start": "func start",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "applicationinsights": "^2.9.1",
        "@azure/web-pubsub": "^1.1.1"
    },
    "devDependencies": {
        "@azure/functions": "^4.0.0",
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0",
        "vitest": "^1.0.0"
    }
}