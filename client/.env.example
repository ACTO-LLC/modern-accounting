# Azure AD / Entra ID Configuration
# Copy this file to .env.local and fill in your values

# Azure AD App Registration Client ID (Application ID)
VITE_AZURE_CLIENT_ID=your-client-id-here

# Azure AD Authority URL
# For single tenant: https://login.microsoftonline.com/{tenant-id}
# For multi-tenant: https://login.microsoftonline.com/common
# For Azure AD B2C: https://{tenant}.b2clogin.com/{tenant}.onmicrosoft.com/{policy}
VITE_AZURE_AUTHORITY=https://login.microsoftonline.com/common

# Redirect URI (should match App Registration)
VITE_REDIRECT_URI=http://localhost:5173

# API Scopes for accessing the DAB API
# Format: api://{api-client-id}/access_as_user
VITE_API_SCOPES=api://your-api-id/access_as_user
