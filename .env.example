# Modern Accounting - Environment Variables
# Copy this file to .env and update with your actual values

# Database Configuration
# Used by Docker Compose, DAB, and deployment scripts
# For local development, use a strong password (minimum 8 chars, uppercase, lowercase, number, special char)
SQL_SA_PASSWORD=YourStrongPasswordHere123!

# SQL Server Connection Details (for local development)
SQL_SERVER=localhost
SQL_PORT=14330
SQL_USER=sa
SQL_DATABASE=AccountingDB

# Azure Configuration (for production)
# These should be set in your Azure environment or deployment pipeline
# AZURE_SQL_CONNECTION_STRING=Server=your-server.database.windows.net;Database=AccountingDB;...
# CORS_ORIGINS=https://your-domain.com
# AZURE_AD_AUDIENCE=your-audience-id
# AZURE_AD_ISSUER=https://login.microsoftonline.com/your-tenant-id/v2.0
